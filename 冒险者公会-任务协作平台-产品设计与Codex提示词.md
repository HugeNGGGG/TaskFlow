
# 项目：冒险者公会 · 内部任务协作平台（Task Guild）

## 一、背景与目标

我要做一个公司内部的任务协作工具，覆盖「任务接取分配、进度实时查询、任务文件上传下载留档、任务进度总览、员工接取任务统计」五大核心诉求，交付形态为 **Web 网页端 + 微信小程序** 双端。

整体视觉与交互主题定位为 **游戏中的「冒险者协会」任务板**：管理者是"公会会长/发布官"，员工是"冒险者"，任务是"委托"，完成任务获得经验、积分、称号。让派活接活这件事清晰、有趣、有即时反馈。

- 团队规模：**小团队（<50 人）**
- 游戏化深度：**主题视觉 + 等级/积分/称号**（不要做成复杂重度游戏，保持轻量、易落地）

## 二、角色与权限

1. **冒险者（Member / 普通员工）**：浏览与接取公开任务、接收指派、提交进度与成果、查看个人战绩。
2. **发布官 / 会长（Manager）**：发布与分配任务、审核成果、查看全员总览与统计、管理公告。
3. **大管理员（Admin）**：人员/部门管理、角色分配、系统配置。

> 权限用 RBAC 位标记即可，一人可兼多角色（小团队常见）。不要求复杂组织架构，但需支持"部门"字段用于筛选统计。

## 三、核心功能需求（请逐项实现并给出验收标准）

### 1. 任务大厅（委托板，首页）
- 任务以卡片形式展示，卡片含：标题、类别、难度等级（D/C/B/A/S）、经验奖励、截止时间、接取人数（如 0/3）、状态徽标。
- 三类任务：悬赏委托（公开自由接取）、指派委托（定向指定）、紧急委托（置顶高亮 + 经验加成）。
- 支持按状态/类别/难度/部门/紧急度/关键字筛选，支持按最新/最紧急/奖励最高排序。
- 接取交互：详情 → 一键「接取委托」→ 二次确认 → 状态变为"进行中"。

### 2. 任务发布与分配
- 发布表单字段：标题、描述、类别、难度、经验奖励、截止时间、接取模式（悬赏/指派）、接取人数上限、是否需要审核、附件。
- 指派支持多选成员，支持"组队委托"（多人协作、设队长）。
- 支持草稿；发布后可通知相关成员。

### 3. 任务执行与进度实时查询
- 状态机：待接取 → 进行中 → 待审核 → 已完成（分支：已逾期、已取消）。
- 冒险者可随时更新进度（百分比 + 文字 + 可选附件），形成**时间线日志**。
- 进度实时同步（WebSocket，降级轮询），发布官打开详情即可见最新进度。
- 逾期自动标记并提醒，支持"申请延长时限"（发布官审批）。
- 审核流：提交 → 通过/打回（附理由）→ 打回后回到"进行中"。

### 4. 文件上传 / 下载 / 留档
- 三类附件区：任务资料、过程附件、成果附件。
- 多次提交保留**历史版本**，可回溯下载。
- 支持单文件下载与多文件打包下载（zip）。
- 附件按角色控制可见性/可下载；存储用云对象存储，DB 只存元数据（文件名、大小、上传者、时间、版本号）。

### 5. 任务进度总览（发布官看板）
- 看板视图按状态分列，统计卡片（总委托数、进行中、待审核、已逾期、本周/本月完成）。
- 可视化：类别分布、部门分布、完成趋势图。
- 支持按部门/类别/发布人/时间过滤。

### 6. 员工接取任务统计（冒险者战绩）
- 个人战绩：接取数、完成数、完成率、平均完成时长、累计经验、等级、称号、积分。
- 排行榜（月/季/年）；发布官可查看成员负载、按时交付率、逾期次数。

### 7. 游戏化系统（轻量）
- 等级：见习→初级→中级→高级→精英→大师→传说，经验累积升级。
- 经验规则（可配置）：完成=基础经验；紧急×1.5；按时/提前交付=加成；逾期/打回=扣减或不得。
- 称号/徽章自动颁发：首单、连击、救火、完美交付、高产之星等。
- 个人头像框/等级徽章/称号徽章随等级变化。

### 8. 通知与消息
- 站内通知 + 微信订阅消息（新指派、审核结果、即将逾期）。
- 消息中心支持已读/清空、通知开关。

## 四、页面清单

**Web 端**：登录、任务大厅（首页）、委托详情（进度时间线+附件+审核）、我的委托、个人中心、管理后台（总览看板/委托发布/员工统计/公告消息/系统设置）。

**小程序端**：首页委托板、委托详情（进度+附件上传下载）、我的委托、消息通知、我的（等级/积分/称号/战绩）。

> 小程序端聚焦高频核心操作，复杂管理留在 Web 端。

## 五、非功能需求

- 实时性：进度/状态秒级同步（WebSocket，降级轮询）。
- 安全：登录鉴权（小程序微信登录 / Web 账号密码）、RBAC 权限、附件访问鉴权。
- 性能：小团队规模单机 + 云数据库即可，预留扩展空间。
- 可部署：提供一键部署说明（环境变量 + 初始化脚本）。

## 六、技术选型

**由你在计划中给出选型方案并说明理由**（我没有强制技术栈）。我的倾向参考（可调整）：
- 跨端优先考虑 **uni-app（Vue3）** 或 **Taro（React）**，实现一套代码双端复用，降低小团队维护成本；
- 后端倾向轻量易维护方案（Node/NestJS 或 Python/FastAPI）；
- 数据库 PostgreSQL/MySQL + 云对象存储 + WebSocket。
- 请在计划阶段明确：前后端技术栈、数据库、对象存储、实时方案、部署方式，并说明为何适合小团队。

## 七、工作方式与交付要求（重要）

1. **请先输出完整实施计划**（模块拆分、页面/接口/数据表设计、里程碑），经我确认后再开始编码。
2. 计划需包含：**数据模型设计（表结构）、API 设计、双端页面清单与路由、权限模型、游戏化数值规则表、部署方案**。
3. 建议按里程碑迭代交付（如：M1 任务大厅+发布+接取 → M2 进度+审核+文件留档 → M3 总览+统计 → M4 游戏化+通知+小程序完善）。
4. 每完成一个里程碑，给出可运行验证的产物与自测说明。
5. 请保持代码结构清晰、注释必要处、关键设计有文档说明。

## 八、验收标准（整体）

- 双端可完成：发布任务 → 接取 → 更新进度 → 提交成果 → 审核通过/打回 → 完成归档的完整闭环。
- 文件上传下载留档可用，历史版本可回溯。
- 发布官可实时查看全员任务进度与统计。
- 游戏化（等级/经验/积分/称号）正常结算与展示。
- 提供可一键部署的说明与初始化脚本。